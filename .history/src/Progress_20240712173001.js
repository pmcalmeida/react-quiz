function Progress({ index, numQuestions }) {
    return (
        <header className="progress">
            <p>Question <strong>{i}</strong>  / {numQuestions}</p>
        </header>
    )
}

export default Progress
